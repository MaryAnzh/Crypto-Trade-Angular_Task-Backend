export const {
    GREETING,
    MAIL_EXIST,
    USER_NOT_EXIST,
    INVALID_CREDENTIALS
} = {
    GREETING: 'Hello from app',
    MAIL_EXIST: 'Email already in use',
    USER_NOT_EXIST: 'User not exist',
    INVALID_CREDENTIALS: 'Invalid credentials'
} as const;